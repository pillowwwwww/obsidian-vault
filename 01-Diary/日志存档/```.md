---
date: 2025-08-14 23:33:05
tags: dailynote
rating: ⭐
excerpt: 
---

## TODO

- [ ] 任何事情都能在五小时之内做完！
- [ ] 多喝水，避免久坐不动
- [ ] 阅读一篇文献

## Tracking

### 第一步：找instruction
```
---
我现在在用 instruction–answer–concept/theorem 框架去测试模型的 deep research 能力（在测试时我不会上传这篇论文）

任务描述：
我将提供一篇Thesis（硕博学位论文）。请你作为一位深耕领域前沿的学者，找出Thesis中解决的instruction（关键问题），该instruction能够在Thesis中找到一个结构简单的answer（答案），再找出解决instruction得到answer所需要的3-5条Concept/Theorem知识点，并重点检查以下方面的问题：
---
 instruction 应该是一个论文中确实解决了的、但能用一个非常简单的量或结论来回答的问题。不能过于开放。
---
请检查answer的结构是否符合结构简单的要求，answer的形式应该是是标量、产物名、简短的结论等。
---
请检查每一条Concept/Theorem是否出现在Thesis中，并且是解决instruction所必须。每一条Concept/Theorem中只包含一个概念或定理。
---
输出格式建议：
请按照以下结构回答：
Instruction：（领域前沿的问题）
Answer：（结构简单的答案）
Concept/Theorem：（解决instruction得到answer所需要的知识点）
- concept_n:...
- theorem_n:...
（以此类推）
```


### 第二步：roll answer:
```
---
但是这次的answer结构还是有点难，你可以换一个题目使得answer的结构尽量简单吗？
请注意，不要违背我的要求
---
```

### 转换markdown
```
将下面的内容转为我可以复制的markdown格式，请小心处理其中的数学符号，与原文一样，不要修改：
```

### instruction问豆包
```
任务描述：
我将提供一个Instruction（前沿问题）、若干个解决Instruction需要用到的Concept/Theorem（概念、定理），请回答Instruction。
---
Concept/Theorem:
"""
1. GRS 列表译码（Guruswami–Sudan）
**构造/公式**：存在确定性 ListDecode 算法，对任意  
\( z \in F_q^N \)，在多项式时间内产出所有与 \( z \) 的汉明距离  
\( \le N-kN \) 的 GRS 码字列表：

\[
ListDecode(z) \;\to\; \{x \in GRS_{F_q,\gamma,k,v} : hw(x-z) \le N-kN\}.
\]

**复杂度**：\( poly(N) \)。

---

2. 折叠 RS 的列表可恢复性
**定义**：码 \( C \subseteq \Sigma^n \) 为 \((\zeta,\ell,L)\)-list-recoverable，若对任意 \( |S_i| \le \ell \) 有

\[
\bigl|\{x \in C : |\{i \in [n]: x_i \in S_i\}| \ge \zeta n\}\bigr| \le L.
\]

**参数化条件（对折叠 RS）**：若  

\[
\zeta N m \;\ge\; (1+\tfrac{r}{s}) \cdot \bigl(\tfrac{N\ell}{k^s}\bigr)^{\tfrac{1}{s+1}} \cdot m^{-s+1},
\quad (r+s)\cdot\bigl(\tfrac{N\ell}{k}\bigr)^{\tfrac{1}{s+1}} < q,
\]

则  
\[
C = RS_{F_q,\gamma,k}^{(m)}
\]
为 \((\zeta,\ell,q^s)\)-list-recoverable。

---

3. 对偶码在偏置分布下的“唯一可译码”与 Decode 算法
**输入偏置**：令 \( D_n \) 为乘积分布，坐标独立地以概率 \( 1-p \) 取 \( 0 \)，否则均匀取 \( \Sigma \setminus \{0\} \)。  

**算法 Decode(z)**：  
1. 展开 \( z \mapsto z^u \in F_q^N \)；  
2. 运行 \( ListDecode(z^{\mathrm{u}}) \) 得到候选列表；  
3. 若存在唯一 \( x \) 使 \( hw(z^u-x) \le (p+\varepsilon)N \)（\(\varepsilon=0.01\)），则输出其折叠 \( x^f \)，否则输出 \( \perp \)。  
**结论（取 \( p \le 0.04 \)）**：  
\[
\Pr_{e \sim D_n}[\,\forall x \in C_n^\perp : Decode(x+e)=x\,] = 1-2^{-\Omega(n)}.
\]

（式上概率为唯一可译码成功率。）

---

4. 乘积分布的直接乘积放大
设在固定哈希下，单实例成功率 \(\le \rho < 1\)。  
对 **同一哈希** 并行 \( t \) 次，成功率满足  

\[
Pr[\text{全对}] \;\le\; \rho^t \;\le\; 2^{-\Omega(t)}.
\]

（用于把单实例“亚常数成功率”放大到并行 \( t \) 次的指数小成功率。）

---

5. \(\lambda(n)\)-重独立哈希族（低次数多项式实现）
**性质**：对任意互异 \( i_1,\dots,i_k \)（\( k \le \lambda(n) \)），有  

\[
(h(i_1),\dots,h(i_k)) \equiv U_k \quad （与独立均匀分布同分布）,
\]

且可用有限域的低次数多项式高效实现；在本题中允许 Alice、Bob 从该族选同一 \( h \) 并做按位 XOR。    
"""

Instruction：
"""
给定如下总两方搜索问题 **T-BiNC（Total Bipartite NullCodeword）**：  
采用一族折叠 Reed–Solomon 码 \( C_n \)（具备强列表可恢复性，且其对偶码在独立 \( p \)-偏置分布下可高概率唯一译码）；  
双方可从 \( k \)-重独立哈希族中共同选择同一哈希，并在同一哈希下并行求解 \( t = poly(n) \) 个实例。  

请给出该问题在两种模型下的通信复杂度有序对：  

\[
(\ \text{QSMP通信量},\ \text{经典双向通信量下界}\ ).
\]

\(( \text{QSMP通信量},\ \text{经典双向通信量下界} ).\)

"""

```


{concept_1:[IEEE Xplore Full-Text PDF:](https://ieeexplore.ieee.org/stamp/stamp.jsp?tp=&arnumber=89518), 
concept_2:[Zero Knowledge Protocols from Succinct Constraint Detection](https://eprint.iacr.org/2016/988.pdf)
concept_3:[null2.pdf](https://web.math.princeton.edu/~nalon/PDFS/null2.pdf)
concept_4: [eprint.iacr.org/2023/587.pdf](https://eprint.iacr.org/2023/587.pdf),[alg.dvi](https://www.scottaaronson.com/papers/alg.pdf)
concept_5:[H:\journals\Springer\Space\37\136\Stage200\Contents\TE\new\cc_MAP.dvi](https://dspace.mit.edu/bitstream/handle/1721.1/116525/37_2016_136_ReferencePDF.pdf?sequence=2&isAllowed=y)
}

_[https://eccc.weizmann.ac.il/report/2016/061/download/, [1610.03798](https://arxiv.org/pdf/1610.03798)]_