---
date: 2025-08-14 23:33:05
tags: dailynote
rating: ⭐
excerpt: 
---

## TODO

- [ ] 任何事情都能在五小时之内做完！
- [ ] 多喝水，避免久坐不动
- [ ] 阅读一篇文献

## Tracking

### 第一步：找instruction
```
---
我现在在用 instruction–answer–concept/theorem 框架去测试模型的 deep research 能力（在测试时我不会上传这篇论文）

任务描述：
我将提供一篇Thesis（硕博学位论文）。请你作为一位深耕领域前沿的学者，找出Thesis中解决的instruction（关键问题），该instruction能够在Thesis中找到一个结构简单的answer（答案），再找出解决instruction得到answer所需要的3-5条Concept/Theorem知识点，并重点检查以下方面的问题：
---
 instruction 应该是一个论文中确实解决了的、但能用一个非常简单的量或结论来回答的问题。不能过于开放。
---
请检查answer的结构是否符合结构简单的要求，answer的形式应该是是标量、产物名、简短的结论等。
---
请检查每一条Concept/Theorem是否出现在Thesis中，并且是解决instruction所必须。每一条Concept/Theorem中只包含一个概念或定理。
---
输出格式建议：
请按照以下结构回答：
Instruction：（领域前沿的问题）
Answer：（结构简单的答案）
Concept/Theorem：（解决instruction得到answer所需要的知识点）
- concept_n:...
- theorem_n:...
（以此类推）
```


### 第二步：roll answer:
```
---
但是这次的answer结构还是有点难，你可以换一个题目使得answer的结构尽量简单吗？
请注意，不要违背我的要求
---
```

### 转换markdown
```
将下面的内容转为我可以复制的markdown格式，请小心处理其中的数学符号，与原文一样，不要修改：
```

### instruction问豆包
```
任务描述：
我将提供一个Instruction（前沿问题）、若干个解决Instruction需要用到的Concept/Theorem（概念、定理），请回答Instruction。
---
Concept/Theorem:
"""

- **concept1：YZ 两阶段量子算法的“非自适应查询 + 后处理”结构**（Yamakawa–Zhandry 2024）  
  **做法**：对每个 $H_i:\Sigma\!\to\!\{0,1\}$ 先**非自适应**地产生  

  $$
  \lvert\phi_i\rangle\ \propto\ \sum_{e\in\Sigma:\ H_i(e)=0}\ \lvert e\rangle,
  $$

  随后进行**一次性后处理**（不再访问 $H$）：把所有 $\lvert\phi_i\rangle$ 与码空间叠加态  
  $\lvert\psi\rangle\propto\sum_{x\in C}\lvert x\rangle$  
  送入同一流程（QFT$/\Sigma^n$+ 编码空间上的线性酉 + 测量）。  
  
- **concept2：码空间量子傅里叶变换（QFT over $\Sigma^n$）的“偏置谱”特性**（标准量子傅里叶工具）  
  **公式**：若单维偏置为 $p:=\Pr[H_i(e)=1]$，则  

  $$
  \widehat{\mathbf{1}_{\{H_i(\cdot)=0\}}}(0)=1-p,\quad
  \sum_{z\ne 0}\bigl|\widehat{\cdot}(z)\bigr|^2=p(1-p),
  $$

- **concept3：GRS（广义 RS）列表译码黑箱**（Guruswami–Sudan 1999）  
  **能力**：给定 $z\in\mathbb{F}_q^N$，在 $\mathrm{poly}(N)$ 时间内枚举所有与 $z$ 的距离 $\le N-\sqrt{kN}$ 的 GRS 码字：  

  $$
  \textsf{ListDecode}(z)\Rightarrow\{x\in \mathrm{GRS}_{q,\gamma,k,\mathbf v}:\ \mathrm{hw}(x-z)\le N-\sqrt{kN}\}.
  $$


- **concept4：折叠 RS 的列表可恢复性（list-recoverability）**（Guruswami–Rudra 2008；Rudra 2007）  ：若  
  $$
  \frac{\zeta N}{m}\ \ge\ \Bigl(1+\frac{s}{r}\Bigr)\!\cdot\!\Bigl(N\ell\,k^s\Bigr)^{\!\frac{1}{s+1}}\!\frac{1}{m-s+1},\qquad
  (r+s)\!\cdot\!\Bigl(\frac{N\ell}{k}\Bigr)^{\!\frac{1}{s+1}}<q,
  $$

  则 $RS^{(m)}$ 为 $(\zeta,\ell,\,q^s)$-list-recoverable。  

- **concept5：“结构 vs 随机性”规约到子立方状协议**（Wang–Yang–Zhang 2023；Yang–Zhang 2024；基于 GPW 提升）  
  **结论形态**：任意经典双向协议可在不显著增大通信的前提下，规约为**子立方状**：  

  * 结构：某些坐标集 $I,J$ 被**完全固定**；  
  * 随机：其余坐标在**最小熵**意义下是 $\gamma$-稠密（每个未固定位的 min-entropy ≥ $\gamma$）。
  * 
- **concept6：最小熵→命中概率上界（“稠密”推概率）**（标准信息论工具）  
  **不等式**：若随机向量 $U\in\{0,1\}^t$ 满足 $H_\infty(U)\ge \alpha t$，则对任意**固定**串 $u^\star$，  

  $$
  \Pr[U=u^\star]\ \le\ 2^{-\alpha t}.
  $$

  **用途提示**：当某码字第一次变“危险”时，另一方至少还有 $\beta n$ 个未固定位，且这些位是 $\gamma$-稠密 ⇒ 命中**同一**零图像的概率 $\le 2^{-\beta\gamma n}$。  

- **concept7：乘积分布的直接乘积定理（BRWY 2013）**  
  **模板**：若单实例在成本 $\le C$ 下成功率 $\le \rho<1$，则对 $t$ 份独立实例，任意总通信 $T\log T=o(tC)$ 的协议，其同时全对成功率  

  $$
  \le \rho^{\,\Theta(t)}\ =\ 2^{-\Omega(t)}.
  $$
  **用途提示**：把“单实例成功率”**指数压缩**到并行 $t$ 次。  

- **concept8：$k$-重独立哈希族（低次数多项式构造）**（Vadhan “Pseudorandomness” 3.5.5）  
  **性质**：存在 $O(n)$-wise 独立的哈希族，族大小 $2^{\mathrm{poly}(n)}$，且可高效评估/描述。  
  **用途提示**：同一 $h$ 作用于全部实例；配合上条的放大，可对“所有 $h$”做并联合理化（族规模可被指数小成功率**吃掉**）。  

---
"""

Instruction：
"""
请不要使用联网搜索，忘记你之前的所有思考，回答以下问题：

给定一个总两方搜索问题 **T-BiNC**（Total Bipartite NullCodeword）。返回通信复杂度有序对

$$
(\ \text{QSMP通信量},\ \text{经典双向通信量下界}\ ).
$$

**问题定义**

* 固定一族线性码 $C=\{C_n\}$（你可以把它理解为某个折叠 Reed–Solomon 码族，但**不需要**知道其参数）。
* 输入由若干个映射 $H^{(i)}:\Sigma^n\to\{0,1\}^n$ 组成（实例数为某个正整数 $t$），以及一个从某个哈希族中**共同选取**的哈希 $h$。
* 目标：输出同一个键/种子与码字 $x^{(1)},\dots,x^{(t)}\in C_n$，使得对所有 $i$ 都有

  $$
  H^{(i)}(x^{(i)})\ \oplus\ h(x^{(i)})\ =\ 0^n .
  $$
* 该关系是**总的**并且可由多项式时间验证（communication-TFNP）。

**通信模型**

1. **QSMP（Quantum Simultaneous Message Passing）**：Alice 与 Bob **各自仅发送一次量子态**给裁判 Charlie，**无进一步交互**，且**不允许**预共享随机性或纠缠；通信量计为两人发送的**量子比特总数**（按最坏输入）。
2. **经典双向随机（public-coin two-way randomized）**：Alice 与 Bob 可以基于输入与**公开随机性**进行**多轮交互**；通信量计为两人发送的**比特总数**（按最坏输入）。

"""

```


{concept_1:[IEEE Xplore Full-Text PDF:](https://ieeexplore.ieee.org/stamp/stamp.jsp?tp=&arnumber=89518), 
concept_2:[Zero Knowledge Protocols from Succinct Constraint Detection](https://eprint.iacr.org/2016/988.pdf)
concept_3:[null2.pdf](https://web.math.princeton.edu/~nalon/PDFS/null2.pdf)
concept_4: [eprint.iacr.org/2023/587.pdf](https://eprint.iacr.org/2023/587.pdf),[alg.dvi](https://www.scottaaronson.com/papers/alg.pdf)
concept_5:[H:\journals\Springer\Space\37\136\Stage200\Contents\TE\new\cc_MAP.dvi](https://dspace.mit.edu/bitstream/handle/1721.1/116525/37_2016_136_ReferencePDF.pdf?sequence=2&isAllowed=y)
}

_[https://eccc.weizmann.ac.il/report/2016/061/download/, [1610.03798](https://arxiv.org/pdf/1610.03798)]_