---
date: 2025-08-21 21:22:32
tags: dailynote
rating: ⭐
excerpt: 
---

## TODO

- [ ] 任何事情都能在五小时之内做完！
- [ ] 多喝水，避免久坐不动
- [ ] 阅读一篇文献

## Tracking
### instruction问豆包
```
任务描述：
我将提供一个Instruction（前沿问题）、若干个解决Instruction需要用到的Concept/Theorem（概念、定理），请回答Instruction。
---
Concept/Theorem:
"""

**C1｜“查询上界”参数的正式引入（query bound q⋆q^{\star}）**

- 记号：对一个 PCP 系统，**对手/验证者读取的比特数**受**参数** q⋆q^{\star} 约束；主文直接表述“**对任何读取至多 q⋆q^{\star} 比特的对手**保持完美零知识”。  
    （文中示例：NP ⊆\subseteq PZK-PCP[log⁡n,1][\log n,1]，“against any adversary reading at most q⋆q^{\star} bits of the proof”。）  
    

**C2｜BFLS 基线：NEXP→\to求和断言 + “三次开点”**

- 构造：见证 AA 的多线性扩展 A^\hat A 与算术化 B^\hat B。需检查的断言为
    
    ∑z∈{0,1}r ∑b1,b2,b3∈{0,1}sB^(z,b1,b2,b3,A^(b1),A^(b2),A^(b3))=2r+3s.\sum_{z\in\{0,1\}^r}\ \sum_{b_1,b_2,b_3\in\{0,1\}^s} \hat B\big(z,b_1,b_2,b_3,\hat A(b_1),\hat A(b_2),\hat A(b_3)\big)=2^{r+3s}.
- 记账：BFLS 用 sumcheck **把验证化为对 A^\hat A 的 3 个随机点评估**。  
    
    

**C3｜Sumcheck 承诺（commitment）与“阈值隐藏”**

- 定义：对每个 α∈Fs\alpha\in\mathbb{F}^s，承诺多项式 C(α,⋅)C(\alpha,\cdot) 满足
    
    ∑c∈{0,1}k ⁣C(α,c)=A^(α).\sum_{c\in\{0,1\}^{k}}\! C(\alpha,c)=\hat A(\alpha).
- **阈值性质（perfectly hiding）**：**任意对手若对 CC 的查询数 t<2kt<2^{k}**，其观察分布与 A^\hat A **无关**（完美隐藏）。  
    
    

**C4｜用 ZK 的 sumcheck-PCP 替换标准 sumcheck（避免泄露部分和）**

- 做法：将 BFLS 的标准 sumcheck 替换为 **[GOS24]** 的**零知识** sumcheck-PCP 组件；该组件隐藏“难计算的部分和”。  
    
    

**C5｜“局部可计算”与组合后的**查询记账**（常数因子放大）**

- 组合语句：若 (P0,V0)(P_0,V_0) 为 PZK-PCP、**查询上界**为 q⋆q^{\star}，且 PP **ℓ\ell-locally computable** 于 P0P_0，则存在模拟器使组合后系统保持 PZK，且**每个外层查询引发至多 ℓ\ell 次内层访问**。
    
- **记账公式**（从模拟条件改写）：
    
    tC(n) ≤ γ⋅q⋆(n)其中 γ≤ℓ 为常数。t_C(n)\ \le\ \gamma\cdot q^{\star}(n)\quad\text{其中 }\gamma\le \ell\text{ 为常数。}
    
    证据（等价表述“外层可承受的查询数为 q⋆/ℓq^{\star}/\ell”）：
    

**C6｜字母表约化到布尔并保持 ZK，且**不改变 q⋆q^{\star}**（仅 O(a)O(a) 因子）**

- 引理：若 LL 在字母表 {0,1}a\{0,1\}^a 上有 PZK-PCP（查询复杂度 qq、查询上界 q⋆q^{\star} 等），则 LL 存在**布尔** PZK-PCP，查询复杂度变为 O(a⋅q)O(a\cdot q)，而**查询上界仍为 q⋆q^{\star}**。
    
- 构造：按 **[BGHSV06]** 的 alphabet reduction，配合**线性规模电路可计算**的**良码**（如 **[Spi96]**）  
    ECC:{0,1}a→{0,1}b\mathrm{ECC}:\{0,1\}^a\to\{0,1\}^{b}（b=O(a)b=O(a)），令 τ(γ)=ECC(πa(γ))\tau(\gamma)=\mathrm{ECC}(\pi_a(\gamma))。  

"""

Instruction：
"""
请不要联网搜索，回答下面的问题：

在复杂度类 **NEXP**（nondeterministic exponential time）中，考虑满足以下全部条件的 **PCP**（probabilistically checkable proof）体系：

1. 非交互；2) 验证器**非自适应**（non-adaptive）；3) 证明按**布尔字母表** {0,1}\{0,1\} 编码，验证器只读取比特；4) 对任意**多项式时间**（PPT）的（可恶意）验证者都为**完美零知识**（perfect zero-knowledge：存在 PPT 模拟器能逐位完全复现其观察视图）；5) 允许**指数级**证明长度。  
    该体系用一种针对见证多项式的**和检验承诺**（sumcheck commitment；一种多项式承诺）来实现掩蔽；该承诺对**少于 2k2^{k}** 次对承诺对象 CC 的查询具有**完美隐藏**（perfectly hiding）性质。令 k(n)k(n) 表示为对**任意** PPT 验证者保持完美零知识所需的**最小**承诺参数的渐近增长量级。  
    **问题：请给出 k(n)k(n) 的最紧渐近界（用标准记号）。**

"""

```
