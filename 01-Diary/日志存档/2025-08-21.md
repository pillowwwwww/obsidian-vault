---
date: 2025-08-21 21:22:32
tags: dailynote
rating: ⭐
excerpt: 
---

## TODO

- [ ] 任何事情都能在五小时之内做完！
- [ ] 多喝水，避免久坐不动
- [ ] 阅读一篇文献

## Tracking
### instruction问豆包
```
任务描述：
我将提供一个Instruction（前沿问题）、若干个解决Instruction需要用到的Concept/Theorem（概念、定理），请回答Instruction。
---
Concept/Theorem:
"""
1. **BFLS 基线（NEXP→求和断言 / Sumcheck 骨架）** — _[BFLS91]_  
    **做法**：对 Oracle-3SAT 算术化，令 A^\hat A 为见证多线性扩展、B^\hat B 为布尔电路算术化。验证等价于检查
    
    ∑z∈{0,1}r∑b1,b2,b3∈{0,1}sB^ ⁣(z,b1,b2,b3,A^(b1),A^(b2),A^(b3))=2r+3s.\sum_{z\in\{0,1\}^r}\sum_{b_1,b_2,b_3\in\{0,1\}^s} \hat B\!\big(z,b_1,b_2,b_3,\hat A(b_1),\hat A(b_2),\hat A(b_3)\big)=2^{r+3s}.
    
    **约束**：流程中对 A^\hat A 只需**常数次**随机点评估（“常数开点”），便于用承诺替换开点接口。
    
2. **Sumcheck 承诺（polynomial/sumcheck commitment）与“阈值隐藏”** — *[CFGS22]（亦述 _[CFS17]_）  
    **构造**：对每个 α∈Fs\alpha\in\mathbb{F}^s，生成承诺多项式 C(α,⋅)C(\alpha,\cdot) 使
    
    ∑c∈{0,1}kC(α,c)=A^(α).\sum_{c\in\{0,1\}^{k}} C(\alpha,c)=\hat A(\alpha).
    
    **阈值性质（perfectly hiding）**：任意对手若对 CC 的查询数 t<2kt<2^{k}，其观察分布与 A^\hat A **无关**。  
    （提示：这条给出“查询预算 vs. kk”的唯一门槛公式。）
    
3. **零知识的 sumcheck-PCPP 部件** — _[GOS24]_  
    **做法**：把 BFLS 里“对部分和的检查”替换为**非自适应、完美零知识**的 sumcheck-PCPP，避免泄露中间部分和；查询复杂度为多项式级，后续可经组合降为常数。  
    （提示：保证在“替换 sumcheck”后仍只需通过**局部访问**完成断言的验真。）
    
4. **证明组合与查询记账（constant-factor blow-up）** — _[BGHSV06]_  
    **定义/做法**：若外层 PCP 仅访问其证明的 ℓout\ell_{\text{out}} 个位置，内层 PCPP 对这些位置做局部验证，则组合后**总查询复杂度**由内层主导，对外层仅有**常数因子**放大。  
    **查询记账公式**：存在常数 γ>0\gamma>0，使**对承诺的总访问**满足
    
    tC(n)  ≤  γ⋅q⋆(n).t_C(n)\;\le\;\gamma\cdot q^{\star}(n).
    
    （提示：这是把“恶意验证者的总查询”精确地**迁移**到“对承诺的总查询”的关节点。）
    
5. **字母表约化到布尔并保持零知识** — _[BGHSV06] + [Spi96]_  
    **做法**：选取 ECC:{0,1}a ⁣→ ⁣{0,1}b\mathrm{ECC}:\{0,1\}^{a}\!\to\!\{0,1\}^{b}（常距、常数率、线性规模电路可计算），把大字母表符号编码到比特串。  
    **代价**：查询复杂度仅按 O(a)O(a) 增长（固定实现下是**常数因子**），并**保持**鲁棒与零知识。  
    （提示：保证题干中“布尔字母表、非自适应、非交互”的同时，不改变对 tCt_C 的常数级记账。）
"""

Instruction：
"""
请不要联网搜索，回答下面的问题：

在复杂度类 **NEXP**（nondeterministic exponential time）中，考虑满足以下全部条件的 **PCP**（probabilistically checkable proof）体系：

1. 非交互；2) 验证器**非自适应**（non-adaptive）；3) 证明按**布尔字母表** {0,1}\{0,1\} 编码，验证器只读取比特；4) 对任意**多项式时间**（PPT）的（可恶意）验证者都为**完美零知识**（perfect zero-knowledge：存在 PPT 模拟器能逐位完全复现其观察视图）；5) 允许**指数级**证明长度。  
    该体系用一种针对见证多项式的**和检验承诺**（sumcheck commitment；一种多项式承诺）来实现掩蔽；该承诺对**少于 2k2^{k}** 次对承诺对象 CC 的查询具有**完美隐藏**（perfectly hiding）性质。令 k(n)k(n) 表示为对**任意** PPT 验证者保持完美零知识所需的**最小**承诺参数的渐近增长量级。  
    **问题：请给出 k(n)k(n) 的最紧渐近界（用标准记号）。**

"""

```
