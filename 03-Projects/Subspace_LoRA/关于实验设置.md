1. 极端重尾
2. 需要看lora附录的一些实验设置。
3. 指令微调的数据集用什么？
4. 实验设置分为两类：
	- 一客一任务
	- **4.6 节 "Scalability Study"** 中引入**Dolly-15K** 数据集，一个客户端使用混合任务。 

5. 代码prompt

### Prompt For Vibe Coding Tool

**Context & Objective:**
I am working with the **FlexLoRA** codebase @doc. FlexLoRA currently aggregates heterogeneous LoRA modules by reconstructing the full-size weight update , performing a weighted average, and then using SVD to redistribute ranks.

**My Goal:**
I want to replace the current `Server Aggregation` and `Client Update/Payload` logic with a new algorithm called **"Subspace_LoRA"**. The core problem I am solving is the "Gauge Ambiguity" in LoRA factors and the inefficiency of reconstructing full  matrices.

Please modify the code (specifically the `Client` logic for packing updates and the `Server` logic for aggregation) to implement the following 4-step algorithm. (client.py and  model_aggreagation.py )

---

### Algorithm Implementation Specification

#### 1. Modification to Client-Side Update (Gauge Fixing)

**Current Logic:** The client sends raw LoRA matrices  and  (or the merged ).
**New Logic:** Before sending the update to the server, perform **QR Decomposition** on the LoRA B-matrix to fix the gauge.

* **Input:** Local LoRA matrices  and .
* **Operation:**
1. Perform Reduced QR decomposition on :



Where  is orthogonal () and  is upper-triangular ().
2. Define the local subspace basis: .
3. Absorb the residual magnitude into the A-matrix:




* **Payload to Server:** Send the pair  instead of .

#### 2. Server-Side Step A: Consensus Subspace Construction (The Reference Basis)

**Current Logic:** Weighted average of parameters.
**New Logic:** Construct a global "Reference Subspace" () that represents the consensus of all clients' active directions.

* **Mathematical Goal:** Find the top- eigenvectors of the weighted sum of projectors: .
* **Optimization (Crucial for LLMs):** Do **NOT** compute the  matrix  explicitly (it will OOM). Instead, use the SVD trick:
1. Construct a stacked matrix  by horizontally concatenating scaled client bases:



Shape of  will be .
2. Perform SVD on : .
3. The **Global Reference Basis**  is the top- columns of  (left singular vectors).



#### 3. Server-Side Step B: Coordinate Translation & Aggregation

**New Logic:** Project purely the coefficients () into the shared Reference Basis before averaging.

* **Operation:**
1. For each client :
* Calculate the alignment matrix (projection of local basis onto global basis):


* Translate local coefficients to global coordinates:




2. Aggregate the translated coefficients:





#### 4. Server-Side Step C: Heterogeneous Redistribution

**Current Logic:** SVD on the global weight and truncation.
**New Logic:** Direct truncation of the Reference Basis and Aggregated Coefficients.

* **Operation:**
For a client  requesting rank :
1. **New Global B:**  (First  columns of the global basis).
2. **New Global A:**  (First  rows of the global coefficients).
3. Send  to the client.



---

### Implementation Hints for the Tool

1. **Locating the Injection Points:**
* Look for the `_start_train` or `train` method in the Client class. Intercept the model state dict extraction. Apply the QR decomposition there.
* Look for the `aggregate` function in the Server class. It likely loops through client messages. Replace the existing parameter averaging loop with the "Stack  -> SVD -> Project  -> Average" pipeline described above.


2. **Handling Shapes:**
* Note that clients have different ranks (). When creating the stacked matrix , handle the variable column widths dynamically.
* Ensure `torch.linalg.qr` is used with `mode='reduced'`.


3. **Hyperparameters:**
* Use a global hyperparameter `server_max_rank` (or ) to define the width of . This effectively replaces the implicit rank bounds in FlexLoRA.



Please generate the necessary code changes or new function definitions to implement this logic, replacing the original FlexLoRA aggregation scheme.

-