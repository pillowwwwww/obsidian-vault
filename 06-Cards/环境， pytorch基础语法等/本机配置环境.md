---
Title: 本机配置环境
tags: 
原始链接:
---
本机配置环境

先去anaconda prompt中写init，然后再打开powershell，可能要开权限。
powershell默认开启了（bash）

cuda不可用怎么办：
torch可能安装了torch-only版本

易错：
**在 Conda 环境中，确实可以使用两种安装方式：`conda install` 和 `pip install`**，这并不是矛盾，而是 Conda 设计之初就支持的混合模式。但关键是：

> ✅ **你用哪种方式安装，就要用同一种方式卸载和管理！**