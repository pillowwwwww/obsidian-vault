实验一：
证明：个性化知识在 LoRA 的奇异谱中呈非均匀分布，并在 B 矩阵的中后部子空间中更为集中；相比之下，head 更富含共享适配方向，但仍可能混有部分任务特异成分。
```
你现在是一名顶级的机器学习工程师和科研代码助手。请你帮我在“现有联邦 LoRA 训练代码”上，严格实现一个结构分析实验。

【总目标】
我要验证这样一个结构命题：

“客户端个性化信息更像富集在 LoRA 的 B 矩阵的中尾部奇异子空间中，而不是头部或纯数值噪声区。”。只分析 LoRA 的 B 矩阵，验证“B 的 middle/tail 子空间比 head 更像 client-specific knowledge 的载体”。


已知数据格式：
- 我有 511 个客户端
- 每个客户端对应一个独立 json 文件
- 一个 json 文件就是一个 client dataset
- 不要重建 public/private dataset
- 不要重新构造 json
- 直接基于现有 client json 做实验

实验定义：
我现在不关心 A/B 差异，也不关心性能指标。
我只关心：
在同一个 B 矩阵内部，head / middle / tail 哪个谱带更像 client-specific structure。

严格判据：
一个 band 更像 client-specific structure，当且仅当：
1. same-client reproducibility 高：同一客户端两个独立副本训练得到的该 band 子空间相似
2. diff-client distinguishability 强：不同客户端之间该 band 子空间不相似
3. middle/tail 的这种性质强于 head

训练要求：
- 从 511 个客户端中可配置抽取一部分客户端做实验，例如 16 / 32 / 64
- 每个客户端训练两个独立副本 replica_1 和 replica_2
- 两个副本默认只改随机 seed（seed_only）
- 后续可扩展 bootstrap，但现在不是第一优先

分析对象：
- 只分析 LoRA B 矩阵
- 支持逐层分析
- 支持多层汇总

核心方法：
1. 从每个客户端每个副本的 checkpoint 中提取所有 target modules 的 LoRA B
2. 对每层 B 做 SVD：
   B = U Σ V^T
3. 只分析左奇异子空间 U
4. 不要用单个 rank cosine 作为主指标
5. 必须用 band-level subspace overlap 作为主指标：
   overlap(U1,U2) = ||U1^T U2||_F^2 / k

band 划分：
按有效秩百分位划分，默认：
- head: 前 20%
- middle: 20% - 60%
- tail: 60% - 90%
- floor: 90% - 100%

注意：
- floor 单独记录，但不要作为主结论
- 如果某层有效秩太低，要自动调整 band，避免空区间

核心统计量：
1. same overlap
   S_same(layer, band) = 平均同一客户端两个副本的 overlap

2. diff overlap
   S_diff(layer, band) = 平均不同客户端之间的 overlap

3. PLS
   PLS(layer, band) = S_same - S_diff

4. client identification accuracy
   用某个 band 的 overlap 做最近邻检索：
   用 replica_2 查询，在所有 replica_1 中找最相似客户端
   统计是否能找回自己

我想要的理想结果：
- head：same 高，但 diff 也较高
- middle/tail：same 仍高，但 diff 明显更低
- 因此 middle/tail 的 PLS 高于 head
- middle/tail 的 client-ID accuracy 高于 head
- floor 若 same 和 diff 都低，则解释为噪声区

实现要求：
1. 先阅读我当前工程，告诉我：
   - 训练入口在哪里
   - checkpoint 怎么保存
   - LoRA B 怎么提取
   - 你准备最小改哪些文件
2. 优先复用已有训练逻辑，不要重写整个框架
3. 尽可能直接给可运行代码，而不是只给伪代码
4. 所有参数可配置：
   - num_clients_to_use
   - client sampling seed
   - training seeds
   - band boundaries
   - eps_ratio for singular value filtering
5. 保存结果到 csv/json
6. 生成图：
   - same vs diff overlap across bands
   - PLS across bands
   - client-ID accuracy across bands

输出步骤：
Step 1. 先总结你对现有工程的理解和改造计划
Step 2. 实现客户端抽样、双副本训练、B 提取、SVD 分析、PLS 统计
Step 3. 给出可运行命令
Step 4. 给出结果文件说明
Step 5. 给出 sanity check 清单

sanity check：
- 同一客户端两个副本不能混入别的客户端样本
- same overlap 必须显著高于 diff overlap 才能支持 client-specific
- 如果某个 band diff 很低但 same 也很低，不要误判为个性化结构
- floor 区域若不稳定，应标记为噪声区
```